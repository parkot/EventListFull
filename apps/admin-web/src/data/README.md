# Data

This folder contains local static data, constants, and seed-like UI values used by EventList Admin Web.

Move API-driven data access into the API/service layer and keep only UI-facing defaults here.
