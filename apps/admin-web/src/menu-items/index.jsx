// project import
import dashboard from './dashboard';
import administrators from './administrators';
import support from './support';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, administrators, support]
};

export default menuItems;
