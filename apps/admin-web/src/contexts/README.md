# Contexts

This folder contains React context providers and related helpers used across EventList Admin Web.

Use contexts for cross-cutting app state and keep feature-specific state close to the feature when possible.
